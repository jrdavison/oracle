# Oracle Chess Agent

## About

A chess agent built using Rust.

## Usage

1. Run the application binary:

    ```cli
    cargo run
    ```

2. Build release optimized binary:

    ```cli
    cargo build --release
    ```
