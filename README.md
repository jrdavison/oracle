# Oracle Chess Agent

## About

A chess agent built using Rust.

## Usage

1. Build with `cargo`:

    ```cli
    cargo build
    ```

2. Run the application binary:

    ```cli
    cargo run
    ```
