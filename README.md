# Oracle Chess Agent

## About

A chess agent built using Rust.

## Usage

1. Run binary for development:

    ```cli
    cargo run
    ```

2. Build optimized release binary:

    ```cli
    cargo build --release
    ```
