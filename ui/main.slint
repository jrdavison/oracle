import { Board } from "./board.slint";
import { EvalBar } from "./dashboard.slint";
import { Palette } from "./globals.slint";

struct OracleState {
    board: [int],
}

export component AppWindow inherits Window {
    in-out property <OracleState> state;
    title: "Oracle Chess Agent";

    height: 640px;
    width: 1280px;
    background: Palette.window_bg;

    HorizontalLayout {
        Board {
            board_state: state.board;
            width: parent.width / 2;
            height: parent.height;
        }
        EvalBar {
            height: parent.height;
            width: 18px;
        }
    }
}
